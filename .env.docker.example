# For Docker: docker compose --env-file .env.docker up -d

# Name prefix for the container and the network
SERVICE_NAME=portfolio

# Docker image
DOCKER_IMAGE=portfolio:latest

# Server
PORT=3000
HOST_PORT=3000

# Host directory holding messages.json, the contact form submissions
DATA_DIR=./data
